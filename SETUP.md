# 🚀 Настройка проекта с автоматической публикацией Allure отчетов

## 📋 Что делает проект

1. ✅ Запускает тесты с Allure
2. ✅ Генерирует Allure отчет
3. ✅ Автоматически публикует на GitHub Pages
4. ✅ Выдает ссылку на отчет

## 🔧 Настройка

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название: `test-project` (или любое другое)
3. Публичный или приватный (на ваше усмотрение)
4. Не добавляйте README, .gitignore, лицензию

### Шаг 2: Подключите локальный репозиторий

```bash
cd ~/test-project
git remote add origin https://github.com/YOUR_USERNAME/test-project.git
git push -u origin main
```

### Шаг 3: Убедитесь, что репозиторий отчетов настроен

Репозиторий `allure-reports` должен быть:
- ✅ Публичным
- ✅ С включенным GitHub Pages (Source: GitHub Actions)
- ✅ С настроенным workflow для публикации

## 🎯 Как использовать

### Локальный запуск тестов:

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите тесты
pytest tests/ --alluredir=allure-results

# Просмотрите отчет локально
allure serve allure-results
```

### Автоматическая публикация:

1. **Сделайте изменения в коде/тестах**
2. **Закоммитьте и отправьте:**
   ```bash
   git add .
   git commit -m "Update tests"
   git push origin main
   ```

3. **GitHub Actions автоматически:**
   - Запустит тесты
   - Сгенерирует Allure отчет
   - Опубликует на GitHub Pages
   - Выдаст ссылку в Actions

4. **Получите ссылку:**
   - Перейдите в Actions → последний workflow
   - В конце будет ссылка: `https://21michael21.github.io/allure-reports/`

## 📊 Структура проекта

```
test-project/
├── tests/
│   ├── test_api.py      # API тесты
│   └── test_basic.py    # Базовые тесты
├── .github/
│   └── workflows/
│       └── test-and-publish.yml  # Автоматизация
├── requirements.txt
├── pytest.ini
└── README.md
```

## 🔗 Ссылка на отчеты

После каждого push:
```
https://21michael21.github.io/allure-reports/
```

## ⚙️ Настройка workflow

Workflow автоматически:
1. Запускает тесты
2. Сохраняет результаты Allure
3. Копирует в репозиторий `allure-reports`
4. Публикует на GitHub Pages
5. Выдает ссылку

## 🎨 Кастомизация

### Добавьте свои тесты:

Создайте файл `tests/test_your_feature.py`:

```python
import pytest
import allure

@allure.epic("Your Feature")
class TestYourFeature:
    @allure.story("Test story")
    def test_something(self):
        with allure.step("Step 1"):
            # Ваш код
            pass
```

### Измените параметры:

В `.github/workflows/test-and-publish.yml` можно:
- Изменить расписание запуска
- Добавить уведомления
- Настроить условия запуска

## ✅ Готово!

Проект настроен и готов к использованию. Просто делайте push, и отчеты будут публиковаться автоматически!

