# 🔐 Настройка Secrets для GitHub Actions

## Проблема
GitHub Actions не может отправить изменения в репозиторий `allure-reports` из-за отсутствия прав.

## Решение

### Шаг 1: Создайте Personal Access Token (PAT)

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Название: `Allure Reports Token`
4. Выберите scope:
   - ✅ `repo` (полный доступ к репозиториям)
5. Нажмите **"Generate token"**
6. **Скопируйте токен** (он показывается только один раз!)

### Шаг 2: Добавьте Secret в репозиторий test-project

1. Перейдите в репозиторий `test-project` на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Name: `ALLURE_REPORTS_TOKEN`
5. Secret: вставьте скопированный токен
6. Нажмите **"Add secret"**

### Шаг 3: Проверьте

После добавления secret:
1. Перейдите в **Actions**
2. Запустите workflow вручную
3. Теперь он должен успешно пушить в репозиторий отчетов

## Альтернативное решение (без PAT)

Если не хотите использовать PAT, можно изменить workflow так, чтобы он работал только в репозитории `allure-reports`:

1. Удалите шаг "Checkout reports repository" из workflow тестов
2. Запускайте workflow напрямую в репозитории `allure-reports`
3. Копируйте результаты вручную или через другой механизм

Но использование PAT - более удобный вариант.

## Важно

- ✅ Токен должен иметь scope `repo`
- ✅ Токен должен быть добавлен как secret `ALLURE_REPORTS_TOKEN`
- ✅ Не коммитьте токен в код!

