# 🧪 Test Project с автоматической публикацией Allure отчетов

Автоматизированный проект для запуска тестов и публикации отчетов на GitHub Pages.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск тестов локально

```bash
pytest tests/ -v --alluredir=allure-results
```

### 3. Просмотр отчета локально

```bash
# Убедитесь, что Allure установлен
export PATH="$HOME/allure/allure-2.24.0/bin:$PATH"

# Запустите сервер
allure serve allure-results
```

## 📊 Автоматическая публикация отчетов

### Как это работает

1. **Push в репозиторий** → автоматически запускается workflow
2. **Workflow выполняет:**
   - Запуск тестов
   - Генерацию Allure отчета
   - Публикацию на GitHub Pages
3. **Отчет доступен:** https://21michael21.github.io/allure-reports/

### Запуск тестов и публикация

Просто сделайте push в репозиторий:

```bash
git add .
git commit -m "Update tests"
git push origin main
```

Workflow автоматически:
- Запустит тесты
- Сгенерирует отчет
- Опубликует на GitHub Pages

## 📁 Структура проекта

```
test-project/
├── tests/
│   ├── test_api.py      # API тесты
│   └── test_basic.py    # Базовые тесты
├── .github/workflows/
│   └── test-and-publish.yml  # Автоматизация
├── requirements.txt
└── README.md
```

## 🔧 Настройка

### Требования

- Python 3.11+
- pytest
- allure-pytest
- GitHub репозиторий с настроенным Secret: `ALLURE_REPORTS_TOKEN`

### GitHub Pages

Отчеты публикуются автоматически в репозиторий `allure-reports`:
- Репозиторий: https://github.com/21michael21/allure-reports
- Отчет: https://21michael21.github.io/allure-reports/

## 📝 Добавление новых тестов

Создайте файл в папке `tests/`:

```python
import pytest
import allure

@allure.epic("Your Feature")
class TestYourFeature:
    @allure.story("Test story")
    def test_something(self):
        with allure.step("Step 1"):
            # Ваш код
            assert True
```

## 🔗 Полезные ссылки

- **Actions:** https://github.com/21michael21/test-project/actions
- **Отчеты:** https://21michael21.github.io/allure-reports/
- **Репозиторий отчетов:** https://github.com/21michael21/allure-reports

## ✅ Что работает

- ✅ Автоматический запуск тестов при push
- ✅ Генерация Allure отчетов
- ✅ Публикация на GitHub Pages
- ✅ История всех тестов
- ✅ Красивые отчеты с графиками и статистикой
